#include <bits/stdc++.h>
typedef long long ll;

using namespace std;

int main()
{
    /// FIFO -> First In First Out
    queue<ll> q1;
    q1.push(10);
    q1.pop();
    q1.front();
    q1.back();
    q1.empty();
    q1.size();
    q1.swap()

return 0;
}
