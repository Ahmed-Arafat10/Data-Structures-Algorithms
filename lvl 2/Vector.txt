#include <bits/stdc++.h>
typedef long long ll;

using namespace std;

int main()
{
    //vector<ll> v1
  // vector<ll> v1 ;
   vector<ll> v1={12,3,4}; // Initialize
   //v1.push_back(1);
   //vector<ll> v1(5,200); // insert 200 5 times
   //v1.push_back(10);
   //v1.emplace_back(10); // not working on some comiplers
   // v1.pop_back();
  // cout<<v1.front(); //O(1)	
   //cout<<v1.back();
    // cout<<v1.at(0);
 // cout<<v1[0];//O(1)	

   //vector<ll>::iterator it;
   vector<ll>::reverse_iterator rit;
    vector<ll>::reverse_iterator rit2;
  // vector<ll> v1::iterator it2 = v1.begin();
  // it = v1.end();// use -1 as it refers to }
  // it = v1.rbegin() ;
//   it = v1.begin();
//   rit = v1.rend()-2;
//   rit2 = v1.rbegin()+1;
//   cout<<*rit2;
//cout<<v1[0];
//-------

//for(ll i=0;i<v1.size();i++) cout<<v1[i]<<" " ;
//cout<<"\n";
//    it = v1.begin();
//
//    for(;it != v1.end();it++) cout<<*it<<" " ;
//
//    auto ii = v1.begin();
//    auto str = "hello";
//    cout<<*ii;
//
//    for(auto it : v1)//Foreach
//        cout<<it<<" ";




 //vector<ll> v2={2,1,3,4},v3={10,11,12,13};
// //v2.insert(v2.begin()+3,14);
// v2.insert(v2.end()-1,14);O(N)
// cout<<v2[4];
// v2.swap(v3);
// for(auto it:v2) cout<<it<<" ";

//Erase A Range
//v2.erase(v2.begin(),v2.begin()+1);O(N) 
//v2.erase(v2.begin(),v2.end());
//Erase Just One Number
//v2.erase(v2.end()-1);
//for(auto it:v2) cout<<it<<" ";

//    v2.clear();O(n)


//reverse(v2.begin(),v2.end()); O(N)


//sort(v2.begin(),v2.end()); // 1 2 3 4
//sort(v2.rbegin(),v2.rend());// 4 3 2 1
//sort(v2.begin(),v2.end(),greater<ll>()); O(n log n) // 4 3 2 1



//vector<ll> v2={2,1,3,4},v3={10,11,12,13};

//auto it = v2.begin();
//cout<<++(*it);
//cout<<*(it++);
//cout<<*(--it);
//for(auto it:v2) cout<<it<<" ";


//    cout<<v1.max_size()<<"\n";
//     cout<<v1.capacity();
// v1.size()
//    v1.shrink_to_fit();
  //  cout<<v1.empty();



    return 0;
}
